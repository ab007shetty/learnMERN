const reactQuestions = [
  {
    id: 1,
    question:
      "What is the Virtual DOM and how does it improve performance in React?",
    answer: [
      "The Virtual DOM is a lightweight copy of the real DOM stored in memory.",
      "React updates the Virtual DOM first instead of directly updating the real DOM.",
      "React compares the old and new Virtual DOM using a process called diffing.",
      "Only the changed parts are updated in the real DOM (efficient patching).",
      "This reduces costly DOM operations and improves performance.",
    ],
    example: [
      "// Virtual DOM concept (simple idea)",
      "const vdom1 = <h1>Hello</h1>;",
      "const vdom2 = <h1>Hello World</h1>;",
      "// React compares vdom1 and vdom2 and updates only the changed text",
    ],
    keyterms: [
      "Virtual DOM = in-memory representation of UI",
      "Diffing = compare old and new VDOM",
      "Reconciliation = update real DOM efficiently",
      "Real DOM = actual browser DOM",
    ],
  },
  {
    id: 2,
    question:
      "What is React Router and what's the difference between dynamic and nested routes?",
    answer: [
      "React Router is a library for handling navigation in React apps. It lets you map URLs to components.",
      "Dynamic Routes use URL parameters to render content based on values in the URL. Example: /user/:id renders different user profiles based on id.",
      "Nested Routes allow you to show child components inside parent routes. Example: /dashboard/settings renders Dashboard as a parent, and Settings inside it.",
    ],
    keyterms: [
      "Key Difference: Dynamic routes change based on the URL parameter, while nested routes represent a hierarchical structure of components.",
    ],
  },
  {
    id: 3,
    question:
      "What is the difference between authentication and authorization in React?",
    answer: [
      "Authentication in React verifies the user’s identity, usually using login forms and tokens (like JWT). Example: After login, the token is stored (e.g., in localStorage) and used in API requests.",
      "Authorization controls what routes or components a user can access based on their role or permissions. Example: A protected route like /admin is only accessible if the user role is “admin”.",
    ],
    keyterms: [
      "Key Difference: Authentication checks who the user is; authorization checks what they're allowed to access in the React app.",
    ],
  },
  {
    id: 4,
    question: "What is lazy loading in React and what is React.lazy?",
    answer: [
      "Lazy loading means loading components only when needed, not during the initial render. It improves performance by reducing bundle size.",
      "React.lazy is a built-in function to implement lazy loading in React. You must wrap it with <Suspense>, which shows fallback content while loading.",
    ],
    example: ["const About = React.lazy(() => import('./About'));"],
    keyterms: [
      "Lazy loading = load on demand",
      "React.lazy = function to load components lazily",
      "<Suspense> = wrapper to handle loading state. This helps speed up the initial page load.",
    ],
  },
  {
    id: 5,
    question: "What is code splitting in React?",
    answer: [
      "Code splitting is a technique to split your React app’s code into smaller bundles that load on demand. It improves performance by reducing the initial load size.",
      "React uses dynamic import() and tools like React.lazy to implement code splitting.",
      "This reduces the bundle size and speeds up rendering.",
    ],
    keyterms: [
      "Code splitting = splitting code into smaller chunks",
      "Dynamic import() = loads modules asynchronously",
      "React.lazy = helps load components only when needed.",
    ],
  },
  {
    id: 6,
    question: "What is a Higher-Order Component (HOC) in React?",
    answer: [
      "A Higher-Order Component is a function that takes a component and returns a new enhanced component. It's used to reuse logic across components.",
      "HOCs add extra props or behavior to the wrapped component.",
    ],
    example: ["const EnhancedComponent = withFeature(OriginalComponent);"],
    keyterms: [
      "HOC = function that wraps a component",
      "Reusability = shares logic without repeating code.",
    ],
  },
  {
    id: 7,
    question: "What are controlled and uncontrolled components in React?",
    answer: [
      "Controlled components have their form data managed by React state. The input's value is set via state and updated with event handlers.",
      "Uncontrolled components manage their own state inside the DOM, accessed via refs.",
      "Controlled components provide better control and validation.",
    ],
    example: [
      "<input value={value} onChange={e => setValue(e.target.value)} />           //Controlled",
      "<input ref={inputRef} />                                                   //Uncontrolled",
    ],
    keyterms: [
      "Controlled = React controls input data via state",
      "Uncontrolled = DOM controls input data, React accesses it via refs.",
    ],
  },
  {
    id: 8,
    question: "When to use ref vs useState in React?",
    answer: [
      "Use useState to store and manage data that affects rendering or UI. When state changes, React re-renders the component. ",
      "Example: form inputs, toggle states.",
      "Use ref to access or store mutable values that don’t cause re-render when changed, like DOM elements or timers. ",
      "Example: focus input, store previous values.",
    ],
    keyterms: [
      "useState = reactive state that triggers re-render",
      "ref = mutable value that persists without re-render. Use ref for DOM access or stable variables; use useState for UI data.",
    ],
  },
  {
    id: 9,
    question:
      "What is the difference between functional and class components in React?",
    answer: [
      "Functional components are simple JavaScript functions that return JSX. They use hooks like useState and useEffect for state and lifecycle.",
      "Class components are ES6 classes extending React.Component and use this.state and lifecycle methods like componentDidMount.",
    ],
    keyterms: [
      "Functional components = simpler, use hooks, preferred now.",
      "Class components = use this, older lifecycle methods.",
      "Functional components are now recommended for new React apps.",
    ],
  },
  {
    id: 10,
    question: "What are lifecycle methods in React?",
    answer: [
      "Lifecycle methods are special functions in class components that run at different stages of a component’s life, like mounting, updating, and unmounting.",
      "Common lifecycle methods: componentDidMount — after component mounts, componentDidUpdate — after updates, componentWillUnmount — before unmounting.",
      "In functional components, these are handled with useEffect hook, which can mimic all lifecycle behaviors.",
    ],
    keyterms: [
      "Mounting = component appears on screen",
      "Updating = component re-renders due to state/props change",
      "Unmounting = component is removed from DOM",
      "useEffect = hook for side effects in functional components.",
    ],
  },
  {
    id: 11,
    question: "What is hydration in React?",
    answer: [
      "Hydration is the process of attaching React's event listeners and state to HTML that was rendered on the server.",
      "It is used in server-side rendering (SSR) to make the static HTML interactive.",
      "Without hydration, the HTML would display correctly but React features like state and events would not work.",
      "Hydration allows React to 'take over' pre-rendered HTML efficiently without re-rendering everything.",
    ],
    example: [
      "// Server-side rendering",
      "const html = ReactDOMServer.renderToString(<App />);",
      "document.getElementById('root').innerHTML = html;",
      " ",
      "// Client-side hydration",
      "ReactDOM.hydrate(<App />, document.getElementById('root'));",
    ],
    keyterms: [
      "Hydration = attaching React to server-rendered HTML",
      "SSR = server-side rendering",
      "Interactive UI = HTML becomes fully functional with React",
      "ReactDOM.hydrate = method used for hydration",
    ],
  },
  {
    id: 12,
    question: "What is reconciliation in React?",
    answer: [
      "Reconciliation is React's process of comparing the new Virtual DOM with the previous one to find what changed.",
      "It uses the diffing algorithm to update only the necessary parts of the real DOM.",
    ],
    keyterms: [
      "Reconciliation = comparing Virtual DOM trees",
      "Diffing algorithm = finds minimal changes. Improves performance by avoiding full DOM updates.",
    ],
  },
  {
    id: 13,
    question:
      "What is useStrict in React and what are common use cases for unit testing?",
    answer: [
      "<React.StrictMode> is a wrapper that activates additional checks and warnings for its children in development mode. It helps identify unsafe lifecycle methods, side effects, and potential bugs.",
      "Use Cases: Detect unexpected side effects, Highlight deprecated APIs, Help with future React updates.",
      "Unit tests check individual components or functions in isolation.",
      "Common use cases: Rendering components with different props, Testing state updates and events (e.g., button clicks), Validating conditional rendering, Checking API call functions with mocks.",
    ],
    keyterms: [
      "StrictMode = helps catch potential issues",
      "Unit test = test one piece of logic/component at a time.",
    ],
  },
  {
    id: 14,
    question: "What are error boundaries, and how do they work?",
    answer: [
      "Error boundaries are special React components that catch errors in child components.",
      "They prevent the whole UI/App from crashing by showing a fallback message.",
      "They catch errors during rendering, lifecycle methods, and constructors of child components.",
      "They do NOT catch errors inside event handlers or async code.",
      "They work only in class components using lifecycle methods.",
    ],
    example: [
      "class ErrorBoundary extends React.Component {",
      "  state = { hasError: false };",
      " ",
      "  static getDerivedStateFromError() {",
      "    return { hasError: true };",
      "  }",
      " ",
      "  render() {",
      "    return this.hasError",
      "      ? <p>Error!</p>",
      "      : this.props.children;",
      "  }",
      "}",
      " ",
      "<ErrorBoundary>",
      "  <MyComponent />",
      "</ErrorBoundary>",
    ],
    keyterms: [
      "Error Boundary = catches UI errors",
      "Fallback UI = shown when error happens",
      "getDerivedStateFromError = updates error state",
    ],
  },
  {
    id: 15,
    question:
      "How do you analyze performance in React and what is the React Profiler?",
    answer: [
      "React Profiler is a dev tool that helps measure render performance of components—how often they render and how long they take.",
      "You can use the Profiler tab in React DevTools to: Identify slow components, Spot unnecessary re-renders, Optimize expensive updates.",
      "You can also use React.memo, useMemo, and useCallback to optimize based on Profiler insights.",
    ],
    keyterms: [
      "Profiler = tool to track render time",
      "Re-render = component renders again due to state/prop change",
      "Helps with performance tuning and improving user experience.",
    ],
  },
  {
    id: 16,
    question:
      "What are browser tools like Lighthouse and how are they useful in React apps?",
    answer: [
      "Lighthouse is a Chrome DevTool that audits your web app for performance, accessibility, SEO, and best practices.",
      "Other useful tools: React DevTools - inspect component tree, props, state, and use Profiler; Network tab — monitor API calls and asset loading; Performance tab - analyze frame rate, paint time, and scripting; Console tab - view errors, warnings, and logs.",
      "These tools help ensure your React app runs fast, is accessible, and debugged efficiently.",
    ],
    keyterms: [
      "Lighthouse = automated audit tool",
      "React DevTools = debug React components",
      "Network/Performance tabs = diagnose speed and loading issues.",
    ],
  },
  {
    id: 17,
    question:
      "What is the difference between debouncing and throttling in React?",
    answer: [
      "Debouncing delays a function until a certain time has passed after the last event. Example: search input waits for user to stop typing.",
      "Throttling limits a function to run at most once in a fixed time. Example: handling scroll or resize events every 300ms.",
    ],
    keyterms: [
      "Debounce = wait for pause",
      "Throttle = run at intervals. Both improve performance by reducing unnecessary function calls.",
    ],
  },
  {
    id: 18,
    question: "What are class and functional components in React?",
    answer: [
      "Class components are ES6 classes extending React.Component. They use this.state for state and lifecycle methods like componentDidMount.",
      "Functional components are simple JavaScript functions that return JSX. They use hooks like useState and useEffect for state and side effects.",
    ],
    keyterms: [
      "Class components = older style, use this, lifecycle methods.",
      "Functional components = simpler, use hooks, recommended now. Functional components are preferred for cleaner and easier code.",
    ],
  },
  {
    id: 19,
    question:
      "What are key lifecycle methods in class components and their functional component equivalents?",
    example: [
      "Class Component Method           |           Functional Component Equivalent (useEffect usage)",
      "componentDidMount                =           useEffect(() => { /* mount code */ }, [])",
      "componentDidUpdate               =           useEffect(() => { /* update code */ })",
      "componentWillUnmount             =           useEffect(() => { return () => { /* cleanup */ } }, [])",
    ],
    keyterms: [
      "componentDidMount runs after first render",
      "componentDidUpdate runs after every update",
      "componentWillUnmount runs before component unmounts",
      "useEffect hook can mimic all these behaviors depending on dependencies",
    ],
  },
  {
    id: 20,
    question:
      "What is the difference between useEffect and useLayoutEffect in React?",
    answer: [
      "useEffect runs after the browser paints the UI. It does not block rendering.",
      "useLayoutEffect runs before the browser paints. It blocks rendering until it finishes.",
      "useEffect is used for data fetching, event listeners, timers, logging, etc.",
      "useLayoutEffect is used when you need to measure DOM size or position before the user sees the screen.",
      "useLayoutEffect is synchronous; useEffect is asynchronous (in behavior).",
    ],
    example: [
      "// useEffect (non-blocking)",
      "useEffect(() => {",
      "  console.log('Runs after paint');",
      "});",
      " ",
      "// useLayoutEffect (blocking)",
      "useLayoutEffect(() => {",
      "  console.log('Runs before paint');",
      "});",
    ],
    keyterms: [
      "useEffect = Runs after paint (non-blocking)",
      "useLayoutEffect = Runs before paint (blocking)",
      "Async behavior = useEffect",
      "Sync behavior = useLayoutEffect",
      "DOM measurements = useLayoutEffect use-case",
    ],
  },
  {
    id: 21,
    question: "Difference between react.memo, useMemo, and useCallback?",
    answer: [
      "react.memo memoizes a **component** and skips re-render if its props don't change.",
      "useMemo memoizes a **value** (like heavy calculations) inside a component.",
      "useCallback memoizes a **function** so the same function reference is reused.",
      "react.memo works at component level, useMemo & useCallback work inside components.",
    ],
    example: [
      "// react.memo → memoize component",
      "const Child = React.memo(function Child({ value }) {",
      "  return <p>{value}</p>;",
      "});",
      " ",
      "// useMemo → memoize value",
      "const expensiveValue = useMemo(() => heavyCalc(num), [num]);",
      " ",
      "// useCallback → memoize function",
      "const handleClick = useCallback(() => setCount(c + 1), [c]);",
    ],
    keyterms: [
      "react.memo = skips re-render when props stay same",
      "useMemo = memoizes computed value",
      "useCallback = memoizes function reference",
      "Memoization = avoiding recalculation or re-creation",
    ],
  },
  {
    id: 22,
    question:
      "How do you use useState without destructuring, and how can you manually access the state and setter?",
    answer: [
      "useState returns an array: [currentState, setStateFunction].",
      "Normally we use array destructuring: const [name, setName] = useState('ani').",
      "But we can also access them manually using array indexing.",
      "arr[0] gives the current state.",
      "arr[1] gives the setter function.",
      "Manual access is not common, but useful when destructuring cannot be used—for example inside loops, dynamic operations, or when creating custom abstractions.",
    ],
    example: [
      "// Using useState WITHOUT destructuring",
      "const arr = useState('Ani');",
      " ",
      "// arr[0] = state value",
      "const name = arr[0];",
      " ",
      "// arr[1] = state setter",
      "const setName = arr[1];",
      " ",
      "function updateName() {",
      "  setName('Shetty');",
      "}",
      " ",
      "// Usage inside component",
      "return (",
      "  <div>",
      "    <p>{name}</p>",
      "    <button onClick={updateName}>Change</button>",
      "  </div>",
      ");",
    ],
    keyterms: [
      "useState = React hook returning an array",
      "Destructuring = syntax to unpack array values",
      "arr[0] = state value",
      "arr[1] = state setter",
    ],
  },
  {
    id: 23,
    question: "What are Pure Components in React?",
    answer: [
      "Pure Components re-render only when props or state change.",
      "They use shallow comparison to skip unnecessary updates.",
      "They improve performance by avoiding wasted re-renders.",
      "Impure components re-render on every parent update, even if data is the same.",
      "PureComponent is for class components; React.memo is for functional ones.",
    ],
    example: [
      "// Pure Component (Class)",
      "class MyComp extends React.PureComponent {",
      "  render() {",
      "    return <h1>{this.props.count}</h1>;",
      "  }",
      "}",
      " ",
      "// Functional Pure Component",
      "const MyComp = React.memo(function ({ count }) {",
      "  return <h1>{count}</h1>;",
      "});",
    ],
    keyterms: [
      "PureComponent = Class component with shallow comparison",
      "React.memo = Functional pure component",
      "Shallow comparison = Checks if values changed",
      "Impure component = Always re-renders",
      "Pure vs Impure = Pure skips updates, impure doesn't",
    ],
  },
  {
    id: 24,
    question: "What are React Fiber and Concurrent Mode?",
    answer: [
      "React Fiber is a new reconciliation engine that breaks rendering work into small units so React can pause, resume, or restart work.",
      "Fiber enables asynchronous rendering, time-slicing, and prioritizing updates.",
      "It improves responsiveness by making rendering more flexible and interruptible.",
      "Concurrent Mode (now called Concurrent Features) uses Fiber to allow React to prepare UI in the background.",
      "It makes apps smoother on slow devices by prioritizing important updates over less important ones.",
      "React does not activate Concurrent Mode automatically; features like useTransition enable it.",
    ],
    example: [
      "// Using concurrent feature: useTransition",
      "function App() {",
      "  const [text, setText] = useState('');",
      "  const [isPending, startTransition] = useTransition();",
      " ",
      "  function handleChange(e) {",
      "    startTransition(() => {",
      "      setText(e.target.value);",
      "    });",
      "  }",
      " ",
      "  return <input onChange={handleChange} />;",
      "}",
    ],
    keyterms: [
      "Fiber = React's rendering engine",
      "Reconciliation = Comparing old and new UI",
      "Concurrent Mode = Rendering in background",
      "useTransition = Marks low-priority updates",
      "Scheduling = Choosing which update runs first",
      "Time-slicing = Splitting work into small chunks so UI stays responsive",
    ],
  },
  {
    id: 25,
    question:
      "What are default props in React? Also, what is the difference between state and props?",
    answer: [
      "Default props are fallback values used when a parent component does not pass specific props.",
      "They ensure components always receive some value and prevent errors.",
      "State is internal data managed within a component and can change over time.",
      "Props are external inputs passed from a parent component and are read-only.",
    ],
    example: [
      "// Default props example",
      "function Button({ label }) {",
      "  return <button>{label}</button>;",
      "}",
      " ",
      "Button.defaultProps = {",
      '  label: "Click Me",',
      "};",
      " ",
      "const [count, setCount] = useState(0); // state",
      "<Counter count={count} /> // props",
    ],
    keyterms: [
      "defaultProps = fallback values for missing props",
      "props = external, immutable inputs",
      "state = internal, mutable data",
      "fallback value = used when parent does not pass a prop",
    ],
  },
  {
    id: 26,
    question: "What is prop drilling, and how can we avoid it?",
    answer: [
      "Prop drilling is passing props through multiple levels of components just to reach a deeply nested child.",
      "It makes code harder to maintain and components get unnecessary props.",
      "We can avoid prop drilling using Context API, which provides global data.",
      "We can also use state management libraries like Redux, Zustand, or Recoil.",
    ],
    example: [
      "// Prop Drilling",
      '<Parent name="John" />',
      "<Child name={name} />",
      "<GrandChild name={name} />",
      " ",
      "// Using Context (no drilling)",
      "const NameContext = createContext();",
      '<NameContext.Provider value="John">',
      "  <GrandChild />",
      "</NameContext.Provider>",
      "useContext(NameContext)",
    ],
    keyterms: [
      "Prop Drilling = passing props multiple levels",
      "Context API = avoids drilling",
    ],
  },
  {
    id: 27,
    question: "What is useContext and how do you use it?",
    answer: [
      "Provides a way to pass data through the component tree without prop drilling.",
      "Create a Context with createContext(), provide value via Provider, consume with useContext().",
      "It is useful for global data like themes, user info, etc.",
      "Good for theme, auth, language — not for frequently changing large state (use redux/zustand for that).",
    ],
    example: [
      "const AuthContext = createContext(null);",
      " ",
      "function App() {",
      "  const user = {name: 'Ani'};",
      "  return (<AuthContext.Provider value={user}><Profile/></AuthContext.Provider>);",
      "}",
      " ",
      "function Profile() {",
      "  const user = useContext(AuthContext);",
      "  return <div>{user.name}</div>; // Ani",
      "}",
    ],
    keyterms: [
      "createContext() = creates context object",
      "Provider = supplies value to subtree",
      "useContext() = consumes context value",
    ],
  },
  {
    id: 28,
    question: "What is the useState hook in React?",
    answer: [
      "useState is a hook that lets you add state to functional components.",
      "It returns a state variable and a function to update it.",
    ],
    example: ["const [count, setCount] = useState(0);", "setCount(count + 1);"],
    keyterms: [
      "useState = state hook for functional components.",
      "Replaces this.state in class components.",
      "Triggers re-render when updated.",
    ],
  },
  {
    id: 29,
    question: "What is the useEffect hook in React?",
    answer: [
      "useEffect runs side-effects in function components, like fetching data or subscriptions.",
      "It can also clean up when the component unmounts or dependencies change.",
    ],
    example: [
      "useEffect(() => { fetchData(); }, []); // runs once",
      "useEffect(() => { console.log(value); }, [value]);",
    ],
    keyterms: [
      "useEffect = lifecycle hook (componentDidMount, etc.).",
      "Accepts dependency array to control when it runs.",
      "Supports clean-up with return function.",
    ],
  },
  {
    id: 30,
    question: "What are custom hooks in React?",
    answer: [
      "Custom hooks are user-defined functions built using React's hooks.",
      "They extract reusable logic so components stay clean.",
      "Useful for state, fetching, timers, events, etc.",
    ],
    example: [
      "// Real-world example: Track window width",
      "function useWindowWidth() {",
      "  const [width, setWidth] = useState(window.innerWidth);",
      " ",
      "  useEffect(() => {",
      "    const handleResize = () => setWidth(window.innerWidth);",
      "    window.addEventListener('resize', handleResize);",
      "    return () => window.removeEventListener('resize', handleResize);",
      "  }, []);",
      " ",
      "return width;",
      "}",
      " ",
      "// Usage",
      "function App() {",
      "  const width = useWindowWidth();",
      "  return <p>Window width: {width}px</p>;",
      "}",
    ],
    keyterms: [
      "Custom hook = reusable logic",
      "Must start with 'use'",
      "Encapsulates state, effects, and events",
    ],
  },
  {
    id: 31,
    question: "Explain the Redux flow.",
    answer: [
      "Redux is a predictable state management library for JavaScript apps.",
      "State updates are done using actions and reducers, and components access data via selectors or mapStateToProps.",
      "The flow follows **unidirectional data flow**.",
      "Steps in Redux flow:",
      "1. **Action**: A plain object describing what happened (e.g., { type: 'INCREMENT' }).",
      "2. **Dispatch**: Send the action to the store using dispatch(action).",
      "3. **Reducer**: A pure function that takes current state and action, and returns new state.",
      "4. **Store**: Holds the entire application state.",
      "5. **Subscribe**: Components subscribe to store changes and re-render with new state.",
    ],
    example: [
      "// Action",
      "const increment = { type: 'INCREMENT' };",
      " ",
      "// Reducer",
      "function counter(state = 0, action) {",
      "  switch(action.type) {",
      "    case 'INCREMENT': return state + 1;",
      "    default: return state;",
      "  }",
      "}",
      " ",
      "// Store",
      "const store = Redux.createStore(counter);",
      "store.dispatch(increment);",
      "console.log(store.getState()); // 1",
    ],
    keyterms: [
      "Action = describes what happened",
      "Dispatch = sends action to store",
      "Reducer = pure function to update state",
      "Store = central state container",
      "Subscribe = listen for state changes",
      "Middleware like thunk used for async logic.",
    ],
  },
  {
    id: 32,
    question: "Why is the 'key' prop important in React lists?",
    answer: [
      "Keys help React identify which list items changed, were added, or removed.",
      "They prevent unnecessary re-renders by giving each item a stable identity.",
      "Without keys, React may reorder or reuse DOM nodes incorrectly.",
    ],
    example: [
      "// Correct: Unique key for each item",
      "items.map(item => (",
      "  <li key={item.id}>",
      "    {item.name}",
      "  </li>",
      "));",
    ],
    keyterms: [
      "key = used by React during diffing",
      "Reconciliation = process of updating DOM efficiently",
      "Stable identity = prevents accidental DOM reuse",
    ],
  },
  {
    id: 33,
    question:
      "How does useEffect work in React, and why is the dependency array important?",
    answer: [
      "Runs side-effects after render (data fetch, subscriptions, DOM ops).",
      "Dependency array controls when the effect runs: [] → once, [a] → when a changes, no array → after every render.",
      "Cleanup function (returned) runs before next effect or on unmount, useful to remove listeners/timers.",
    ],
    example: [
      "useEffect(() => {",
      "  // mount: fetch data",
      "  const id = setInterval(() => console.log('tick'), 1000);",
      "  return () => clearInterval(id); // cleanup on unmount",
      "}, []);",
      " ",
      "useEffect(() => {",
      "  // runs whenever `userId` changes",
      "  fetch(`/api/user/${userId}`).then(r => r.json()).then(setUser);",
      "}, [userId]);",
    ],
    keyterms: [
      "side-effect = work outside render (fetch, timers, subscriptions)",
      "dependency array = controls re-run",
      "cleanup = function returned by effect",
    ],
  },
  {
    id: 34,
    question:
      "What are Context API and useContext? What are their pros, cons, and differences?",
    answer: [
      "Context API lets you create global/shared data using createContext and Provider.",
      "It helps avoid prop drilling by allowing components to access shared values anywhere in the tree.",
      "useContext is a hook used to read the value provided by a Context.",
      "Context API sets up the shared state; useContext consumes the shared state.",
      "Pros: Removes prop drilling, easy global state for themes/auth, simple to implement.",
      "Cons: All consumers re-render on every value change, not suitable for fast-changing data, can cause Provider Hell, reduces component reusability.",
    ],
    example: [
      "// Creating Context (Context API)",
      "const UserContext = createContext();",
      " ",
      "// Providing context",
      "function App() {",
      "  return (",
      '    <UserContext.Provider value="John">',
      "      <Profile />",
      "    </UserContext.Provider>",
      "  );",
      "}",
      " ",
      "// Consuming context using useContext",
      "function Profile() {",
      "  const user = useContext(UserContext);",
      "  return <p>{user}</p>;",
      "}",
    ],
    keyterms: [
      "Context API = createContext + Provider (sets up global shared data)",
      "useContext = Hook to read value from a context",
      "Context API ≠ useContext = One creates/provides, one consumes",
      "Context = Used for global/shared data",
      "useContext = Simplifies reading context inside components",
    ],
  },
  {
    id: 35,
    question: "What is the useMemo hook in React and when should it be used?",
    answer: [
      "useMemo memoizes a computed value.",
      "Useful when a calculation is expensive or when avoiding recalculation on every render.",
      "Returns cached result until dependencies change.",
    ],
    example: [
      "const expensive = useMemo(() => num * 1000, [num]);",
      " ",
      "<div>{expensive}</div>",
    ],
    keyterms: [
      "Memoized value = stored computed result",
      "Recalculated only when deps change",
      "Prevents heavy operations from running every render",
    ],
  },
  {
    id: 36,
    question:
      "What is the useReducer hook in React and when should it be used?",
    answer: [
      "useReducer manages complex state using a reducer function.",
      "Useful when state logic involves multiple sub-values or transitions.",
      "Prefer it for complex state logic, multiple sub-values, or when next state depends on previous state.",
      "Preferred over useState for predictable state updates.",
      "Works similar to Redux pattern inside components.",
    ],
    example: [
      "function reducer(state, action) {",
      "  if (action.type === 'increment') return { count: state.count + 1 };",
      "  return state;",
      "}",
      " ",
      "const [state, dispatch] = useReducer(reducer, { count: 0 });",
    ],
    keyterms: [
      "Reducer = Function that takes state + action → returns new state",
      "Dispatch = Sends actions to reducer",
      "Structured state = Better for complex states",
      "Predictable updates = Pure reducer logic",
    ],
  },
  {
    id: 37,
    question:
      "What is the useCallback hook in React and when should it be used?",
    answer: [
      "useCallback memoizes a function reference so it doesn't recreate on every render.",
      "Useful when passing functions to memoized children to avoid unnecessary re-renders.",
      "Works like useMemo but for functions.",
      "Returns the same function instance until dependencies change.",
    ],
    example: [
      "const increment = useCallback(() => setCount(c => c + 1), []);",
      " ",
      "<Child onClick={increment} /> // Child won't re-render if props same",
    ],
    keyterms: [
      "Memoized function = Function reference stored across renders",
      "Stable reference = Prevents child re-renders with React.memo",
      "Callback identity = Same identity unless dependency changes",
      "Dependency array = Decides when function is regenerated",
    ],
  },
  {
    id: 38,
    question: "What is the useRef hook in React and when should it be used?",
    answer: [
      "useRef creates a mutable object that persists across renders.",
      "Changing ref values does NOT trigger re-rendering.",
      "Useful for accessing DOM elements, storing values, or holding mutable state.",
      "Used when you need to store something that survives renders but does not affect UI.",
    ],
    example: [
      "const inputRef = useRef(null);",
      " ",
      "useEffect(() => {",
      "  inputRef.current.focus();",
      "}, []);",
      " ",
      "<input ref={inputRef} />",
    ],
    keyterms: [
      "Mutable ref = Object with a persistent `.current` property",
      "DOM reference = Getting access to HTML elements",
      "No re-render = Ref changes don’t update UI",
      "Instance variable = Holds data between renders",
    ],
  },
  {
    id: 39,
    question: "What is the useId hook in React and when should it be used?",
    answer: [
      "useId creates a unique, stable ID for a component.",
      "It is needed to avoid ID collisions when multiple components render the same inputs or labels.",
      "Useful for accessibility attributes like htmlFor and aria-describedby.",
      "The ID stays the same across renders and works correctly with SSR.",
    ],
    example: [
      "const id = useId();",
      " ",
      "<label htmlFor={id}>Name</label>",
      "<input id={id} />",
    ],
    keyterms: [
      "Unique ID = avoids duplicates",
      "Stable ID = same on every render",
      "SSR-safe = prevents hydration mismatch",
    ],
  },
  {
    id: 40,
    question: "React class component lifecycle: 3 phases and main methods.",
    answer: [
      "Mounting: constructor → render → componentDidMount (init side-effects).",
      "Updating: shouldComponentUpdate → render → componentDidUpdate (respond to prop/state changes).",
      "Unmounting: componentWillUnmount (cleanup).",
    ],
    example: [
      "class MyComp extends React.Component {",
      "  componentDidMount(){ /* fetch */ }",
      "  shouldComponentUpdate(nextProps){ return true; }",
      "  componentDidUpdate(prev){ /* react to change */ }",
      "  componentWillUnmount(){ /* cleanup */ }",
      "  render(){ return <div/>; }",
      "}",
    ],
    keyterms: [
      "mounting = first render",
      "updating = re-render on prop/state change",
      "unmounting = cleanup phase",
    ],
  },
  {
    id: 41,
    question: "How to achieve conditional rendering in React?",
    answer: [
      "Conditional rendering means showing UI based on conditions.",
      "You can use if-else, ternary operator, logical &&, or switch statements.",
      "React decides which component or element to render depending on the condition.",
    ],
    example: [
      "// Using if-else",
      "if (isLoggedIn) {",
      "  return <Dashboard />;",
      "} else {",
      "  return <Login />;",
      "}",
      " ",
      "// Using ternary operator",
      "return isDark ? <DarkMode /> : <LightMode />;",
      " ",
      "// Using logical AND (&&)",
      "return showMessage && <p>Hello User!</p>;",
      " ",
      "// Using switch",
      "switch (status) {",
      "  case 'loading': return <Loader />;",
      "  case 'error': return <Error />;",
      "  default: return <Home />;",
      "}",
    ],
    keyterms: [
      "Conditional Rendering = showing UI based on conditions",
      "Ternary Operator = short if-else",
      "Logical AND (&&) = render component only if condition is true",
    ],
  },
  {
    id: 42,
    question:
      "Compare Redux and Zustand: why, how, and when to use; mention Redux Toolkit.",
    answer: [
      "Both manage global state; choose based on complexity and team needs.",
      "Redux: predictable, middleware, developer tools, more boilerplate; good for large apps and strict patterns.",
      "Redux Toolkit: reduces boilerplate (createSlice, configureStore) and is recommended standard.",
      "Zustand: minimal API, less boilerplate, hook-based store; good for small-to-medium apps or simple global state.",
    ],
    example: [
      "// Redux Toolkit slice (simplified)",
      "const slice = createSlice({ name:'counter', initialState:0, reducers:{inc: s => s+1} });",
      "store.dispatch(slice.actions.inc());",
      " ",
      "// Zustand simple store",
      "const useStore = create(set => ({count:0, inc: ()=>set(s=>({count: s.count+1}))}));",
      "useStore.getState().inc();",
    ],
    keyterms: [
      "Redux = central store + reducers + actions",
      "Redux Toolkit = official abstraction to simplify Redux",
      "Zustand = small hook-based state library",
    ],
  },
  {
    id: 43,
    question:
      "What are custom hooks, why create them, and a hook example using localStorage?",
    answer: [
      "Custom hooks reuse stateful logic (start name with `use`).",
      "They allow sharing behaviour across components without repeating code.",
      "Good for form logic, localStorage syncing, subscriptions.",
    ],
    example: [
      "function useLocalStorage(key, initial){",
      "  const [val, setVal] = useState(()=>{",
      "    const raw = localStorage.getItem(key);",
      "    return raw ? JSON.parse(raw) : initial;",
      "  });",
      " ",
      "  useEffect(()=> localStorage.setItem(key, JSON.stringify(val)), [key, val]);",
      "  return [val, setVal];",
      "}",
      "// usage: const [name, setName] = useLocalStorage('name','Ani');",
    ],
    keyterms: [
      "custom hook = function using hooks for reusable logic",
      "localStorage sync = persist state to browser storage",
      "encapsulation = hide logic inside hook",
    ],
  },
  {
    id: 15,
    question: "What is lazy loading, code-splitting, and Suspense in React?",
    answer: [
      "Lazy loading means loading a component only when it is needed, not at initial load.",
      "Code-splitting breaks the app bundle into smaller chunks to improve performance.",
      "React.lazy is used for lazy loading components.",
      "Suspense shows a fallback (like a loader) while a lazy component is being loaded.",
    ],
    example: [
      "// Lazy Loading",
      "const About = React.lazy(() => import('./About'));",
      " ",
      "// Using Suspense",
      "<Suspense fallback={<p>Loading...</p>}>",
      "  <About />",
      "</Suspense>",
    ],
    keyterms: [
      "Lazy Loading = load component only when needed",
      "Code-Splitting = split code into chunks",
      "React.lazy = dynamically import components",
      "Suspense = shows fallback while lazy component loads",
    ],
  },
  {
    id: 45,
    question:
      "Explain Virtual DOM, reconciliation, React Fiber and the diffing algorithm (brief).",
    answer: [
      "Virtual DOM: lightweight in-memory representation of UI.",
      "Diffing compares previous and current VDOM to compute minimal DOM updates (reconciliation).",
      "React Fiber is the rewrite for incremental rendering and better scheduling (pausing, prioritizing work).",
      "Diffing uses heuristics (keys, element type) to minimize operations.",
    ],
    example: [
      "When state changes, React creates new VDOM, compares (diff) with previous, then updates only changed DOM nodes (not full re-render).",
      "Using keys in lists helps diffing produce minimal moves: <li key={id}>",
    ],
    keyterms: [
      "Virtual DOM = abstract DOM copy",
      "reconciliation = compute minimal updates",
      "Fiber = scheduler + incremental rendering",
      "keys = help list diffing",
    ],
  },

  {
    id: 46,
    question:
      "Routing and RBAC: how to implement protected routes and role-based access in React?",
    answer: [
      "Use React Router for routes; protect routes by checking auth and roles before rendering component.",
      "RBAC: assign roles to user (admin/user) and conditionally allow access.",
      "Redirect or show unauthorized page when check fails.",
    ],
    example: [
      "function Protected({role, children}){",
      "  const user = useAuth();",
      "  if(!user) return <Navigate to='/login' />;",
      "  if(role && user.role !== role) return <Navigate to='/unauthorized' />;",
      "  return children;",
      "}",
      "<Route path='/admin' element={<Protected role='admin'><Admin/></Protected>} />",
    ],
    keyterms: [
      "React Router = routing library",
      "Protected route = gate component checking auth/role",
      "RBAC = role-based access control",
    ],
  },
  {
    id: 47,
    question:
      "How to handle query params, dynamic routing and route params in React Router?",
    answer: [
      "useParams() reads dynamic segments (/:id).",
      "useSearchParams() or new URLSearchParams reads query strings (?q=abc).",
      "Dynamic routing renders different content based on URL params.",
    ],
    example: [
      "// Route: /user/:id",
      "const { id } = useParams(); // id from path",
      "const [searchParams] = useSearchParams();",
      "const sort = searchParams.get('sort'); // ?sort=asc",
    ],
    keyterms: [
      "useParams = path params",
      "useSearchParams = query params",
      "dynamic route = URL-driven rendering",
    ],
  },
  {
    id: 48,
    question:
      "How do you test React components? What are the commonly used testing libraries?",
    answer: [
      "React components are tested using unit tests, integration tests, and UI behavior tests.",
      "Testing checks rendering, user interactions, props, state updates, and API calls.",
      "Jest is used as the main test runner and assertion library.",
      "React Testing Library (RTL) tests components based on user behavior instead of implementation details.",
      "Enzyme is older and used for shallow rendering, but RTL is now preferred.",
      "End-to-end (E2E) tools like Cypress and Playwright test the app in a real browser.",
    ],
    example: [
      "// React Testing Library example",
      "import { render, screen } from '@testing-library/react';",
      "import App from './App';",
      " ",
      "test('renders heading', () => {",
      "  render(<App />);",
      "  const heading = screen.getByText('Hello');",
      "  expect(heading).toBeInTheDocument();",
      "});",
    ],
    keyterms: [
      "Jest = Test runner and assertion library",
      "React Testing Library = Tests UI like users interact",
      "Enzyme = Older React testing tool",
      "Cypress = Browser-based E2E testing",
      "Unit test = Tests single component or function",
      "Integration test = Tests multiple components together",
      "E2E test = Tests full app in browser",
    ],
  },
  {
    id: 49,
    question: "fetch vs axios: differences and usage examples",
    answer: [
      "fetch: native browser API, returns Response which needs .json() parsing; no built-in interceptors.",
      "axios: library, auto-parses JSON (res.data), supports request/response interceptors, better error handling and older browser support.",
      "Choose axios for extra features, fetch for native no-dep usage.",
    ],
    example: [
      "// fetch",
      "fetch('/api/data')",
      "  .then(res => { if(!res.ok) throw new Error('err'); return res.json(); })",
      "  .then(data => console.log(data))",
      " ",
      "// axios",
      "axios.get('/api/data')",
      "  .then(res => console.log(res.data))",
    ],
    keyterms: [
      "fetch = native, promise-based",
      "axios = feature-rich HTTP client",
      "interceptors = middleware for requests/responses",
    ],
  },
  {
    id: 50,
    question:
      "Why use a key in map(), and how does useState differ from normal variables (including manual array form)?",
    answer: [
      "key uniquely identifies list items so React can track elements across re-renders and perform minimal DOM updates.",
      "useState values persist across renders and trigger re-render when updated; normal variables reset each render and do not trigger updates.",
      "Manual form: const stateArr = useState(0); const count = stateArr[0]; const setCount = stateArr[1]; — same underlying API as destructured form.",
    ],
    example: [
      "// keys in list",
      "items.map(item => <li key={item.id}>{item.name}</li>);",
      " ",
      "// useState manual access (not recommended but possible)",
      "const stateArr = useState(0);",
      "const count = stateArr[0];",
      "const setCount = stateArr[1];",
      "setCount(prev => prev + 1);",
    ],
    keyterms: [
      "key = stable id for list items",
      "re-render = UI update caused by state change",
      "useState = hook that returns [value, setValue]",
    ],
  },
  {
    id: 51,
    question:
      "Why do we use parentheses around JSX inside .map, like .map(user => (<li>{user.name}</li>))?",
    answer: [
      "Parentheses here are not for function parameters.",
      "They are used to wrap the JSX being returned from the arrow function.",
      "This allows writing multi-line JSX without needing the 'return' keyword.",
      "Without parentheses: .map(user => <li>{user.name}</li>) (for single-line).",
      "With parentheses: .map(user => ( <li>{user.name}</li> )) (for multi-line readability).",
    ],
    example: [
      "// Single line (no parentheses)",
      "users.map(user => <li>{user.name}</li>)",
      " ",
      "// Multi-line (with parentheses for JSX)",
      "users.map(user => (",
      "  <li key={user.id}>{user.name}</li>",
      "))",
    ],
    keyterms: [
      "Arrow function = Short function syntax using =>",
      "JSX = JavaScript XML, syntax extension for React",
      "Parentheses around JSX = Groups multi-line JSX for implicit return",
    ],
  },
  {
    id: 52,
    question:
      "In the import statement `import { useState } from 'react';` where does destructuring actually happen?",
    answer: [
      "Destructuring happens inside the curly braces `{ useState }`.",
      "React exports an object, and we extract only the `useState` property.",
      "Same as: `const useState = React.useState` after importing React.",
      "This is object destructuring used directly inside the import statement.",
    ],

    example: [
      "import { useState } from 'react';",
      " ",
      "function Counter() {",
      "  const [count, setCount] = useState(0);",
      "  return <button onClick={() => setCount(count + 1)}>{count}</button>;",
      "}",
      " ",
    ],

    keyterms: [
      "Destructuring = extracting specific properties from an object.",
      "Named import = uses curly braces in import statement.",
      "React export object = contains useState, useEffect, useRef, etc.",
    ],
  },
  {
    id: 53,
    question:
      "Why do async/await and .then().catch() behave differently in React rendering, and how can optional chaining help?",
    answer: [
      ".then().catch() callbacks run after the current synchronous code, so the initial render is safe if state is an empty array/object.",
      "async/await pauses inside the function, but the first render happens immediately, so accessing properties of null state can throw errors.",
      "Optional chaining (user?.login) or a guard (if (!user)) prevents errors when the awaited data hasn't arrived yet.",
    ],
    example: [
      "function Example() {",
      "  const user = null;",
      "  return (",
      "    <div>",
      "      <p>Login: {user?.login || 'Loading...'}</p>",
      "      <p>Name: {user?.name || 'N/A'}</p>",
      "    </div>",
      "  );",
      "}",
    ],
    keyterms: [
      "optional chaining = safely access properties of null/undefined objects",
      ".then().catch() = executes after synchronous code, initial render safe",
      "async/await = may cause initial render issues if state is null",
    ],
  },
  {
    id: 54,
    question: "How does React handle forms?",
    answer: [
      "React handles forms using controlled or uncontrolled components.",
      "Controlled forms use state to manage input values.",
      "Uncontrolled forms use refs to read values directly from DOM.",
    ],
    example: [
      "// Controlled",
      "const [name, setName] = useState('');",
      "<input value={name} onChange={(e) => setName(e.target.value)} />",
      " ",
      "// Uncontrolled",
      "const ref = useRef();",
      "<input ref={ref} /> // get value using ref.current.value",
    ],
    keyterms: [
      "Controlled = state-managed input",
      "Uncontrolled = ref-managed input",
      "onChange = updates state",
    ],
  },
  {
    id: 55,
    question:
      "What causes a React component to re-render, and how can you optimize it?",
    answer: [
      "A component re-renders when its state changes.",
      "It also re-renders when its props change.",
      "Parent re-render can cause child re-render.",
      "Context value change triggers re-render.",
      "To optimize, use React.memo for components.",
      "Use useMemo to memoize values.",
      "Use useCallback to memoize functions.",
      "Avoid unnecessary state updates.",
    ],
    example: [
      "// React.memo prevents re-render if props are same",
      "const Child = React.memo(function Child({ value }) {",
      "  return <p>{value}</p>;",
      "});",
      " ",
      "// useCallback to avoid new function every render",
      "const handleClick = useCallback(() => setCount(c + 1), [c]);",
      " ",
      "// useMemo to avoid recalculating",
      "const result = useMemo(() => heavyWork(x), [x]);",
    ],
    keyterms: [
      "Re-render = React updates UI when data changes",
      "React.memo = prevents re-render if props unchanged",
      "useMemo = memoizes expensive value",
      "useCallback = memoizes function",
    ],
  },
  {
    id: 56,
    question: "What is a Hook in React?",
    answer: [
      "Hooks are special functions in React that let you use state and lifecycle features in functional components.",
      "They remove the need for class components.",
      "Hooks start with the word 'use' (like useState, useEffect).",
      "They help manage state, side effects, refs, context, and performance.",
    ],
    example: [
      "// useState Hook",
      "const [count, setCount] = useState(0);",
      " ",
      "// useEffect Hook",
      "useEffect(() => {",
      "  console.log('Component Mounted');",
      "}, []);",
    ],
    keyterms: [
      "Hooks = functions that add React features to functional components",
      "useState = manages state",
      "useEffect = handles side effects",
      "useRef = holds mutable values",
    ],
  },
  {
    id: 57,
    question: "What is React Router, and how does client-side routing work?",
    answer: [
      "React Router is a library used for navigation in React apps.",
      "It lets you switch pages without reloading the browser.",
      "Client-side routing updates the URL and renders components instantly.",
      "It prevents full page refresh by controlling navigation inside the app.",
    ],
    example: [
      "import { BrowserRouter, Routes, Route } from 'react-router-dom';",
      " ",
      "<BrowserRouter>",
      "  <Routes>",
      "    <Route path='/' element={<Home />} />",
      "    <Route path='/about' element={<About />} />",
      "  </Routes>",
      "</BrowserRouter>",
    ],
    keyterms: [
      "React Router = navigation library for React",
      "Client-side routing = URL changes without page reload",
      "Route = maps URL path to component",
    ],
  },
  {
    id: 58,
    question:
      "How does server-side rendering (SSR) differ from client-side rendering (CSR) in React?",
    answer: [
      "SSR renders HTML on the server before sending it to the browser.",
      "CSR renders the UI fully in the browser using JavaScript.",
      "SSR improves SEO and initial load speed, especially for slow devices.",
      "CSR gives faster navigation after load but has slower first paint.",
      "SSR sends ready HTML, CSR sends a blank HTML and loads JS to build UI.",
    ],
    example: [
      "// SSR example (Next.js)",
      "export default function Page({ data }) {",
      "  return <h1>{data}</h1>;",
      "}",
      " ",
      "export async function getServerSideProps() {",
      "  return { props: { data: 'Hello from server' } };",
      "}",
      " ",
      "// CSR example (React)",
      "function App() {",
      "  const [msg] = useState('Hello from client');",
      "  return <h1>{msg}</h1>;",
      "}",
    ],
    keyterms: [
      "SSR = HTML rendered on server",
      "CSR = HTML rendered in browser",
      "SEO = Improves with SSR",
      "Hydration = Converting server HTML to interactive UI",
    ],
  },
  {
    id: 59,
    question:
      "What is the difference between React Node, React Element, and a React Component?",
    answer: [
      "A React Node is anything React can render — strings, numbers, elements, fragments, portals, components.",
      "A React Element is a plain object describing what you want to see on the UI.",
      "A React Component is a function or class that returns a React Element.",
      "Elements are the result of calling a component, and nodes are the final renderable output.",
    ],
    example: [
      "// React Component",
      "function Button() {",
      "  return <button>Click</button>;  // React Element",
      "}",
      " ",
      "// React Node",
      "<div>Hello</div>  // This is a renderable node",
    ],
    keyterms: [
      "React Node = Anything renderable",
      "React Element = JS object describing UI",
      "React Component = Function or class returning an element",
      "Component → returns Element → becomes Node on screen",
    ],
  },
  {
    id: 60,
    question:
      "What is the consequence of using array indices as the value for keys in React?",
    answer: [
      "Using array indices as keys can break React's ability to correctly track list items.",
      "It causes wrong re-renders when list items are reordered, inserted, or deleted.",
      "Components may keep wrong state because React thinks items didn't change.",
      "It leads to UI bugs, unexpected behavior, and poor performance.",
      "Always use a unique and stable key instead of index.",
    ],
    example: [
      "// Bad: using index as key",
      "items.map((item, i) => <li key={i}>{item}</li>)",
      " ",
      "// Good: using unique id",
      "items.map((item) => <li key={item.id}>{item.name}</li>)",
    ],
    keyterms: [
      "Key = Unique identifier for list items",
      "Index key issue = Wrong component state mapping",
      "Stable key = Consistent across renders",
      "Reorder bug = UI mismatch when items move",
    ],
  },
  {
    id: 61,
    question: "What are the rules of React hooks?",
    answer: [
      "Hooks must be called at the top level of a function component.",
      "Hooks must not be called inside loops, conditions, or nested functions.",
      "Hooks must only be called inside React function components or custom hooks.",
      "Custom hooks must start with the word 'use'.",
      "React relies on the order of hooks, so the calling pattern must stay consistent.",
    ],
    example: [
      "// Correct",
      "function App() {",
      "  const [count, setCount] = useState(0);",
      "  return <p>{count}</p>;",
      "}",
      " ",
      "// Wrong: inside condition",
      "function App() {",
      "  if (true) {",
      "    const [count, setCount] = useState(0);  // ❌ Not allowed",
      "  }",
      "}",
    ],
    keyterms: [
      "Top-level only = Hooks must not be inside loops/conditions",
      "React component only = Hooks used in components or custom hooks",
      "Custom hook naming = Must start with 'use'",
      "Order matters = React tracks hooks by call order",
    ],
  },
  {
    id: 62,
    question:
      "Why does setState() accept a callback function, and when should you use that callback form?",
    answer: [
      "The callback form of setState updates state based on the previous state.",
      "It ensures you always get the latest state value because state updates are asynchronous.",
      "Use this format when the new state depends on the old state (like counters or toggles).",
      "Using setState(newValue) directly may cause incorrect updates when multiple updates happen quickly.",
    ],
    example: [
      "// Correct: using callback form",
      "setCount(prev => prev + 1);",
      " ",
      "// Wrong: may use stale value",
      "setCount(count + 1);",
    ],
    keyterms: [
      "Async state = React batches updates",
      "Callback form = Safely uses previous state",
      "Prev state logic = Needed for counters, toggles",
      "Stale state = Wrong value when updates are fast",
    ],
  },
  {
    id: 63,
    question: "What are React Fragments used for?",
    answer: [
      "React Fragments let you group multiple elements without adding extra HTML nodes.",
      "They help keep the DOM clean by avoiding unnecessary <div> wrappers.",
      "Useful when returning multiple elements from a component.",
      "Short syntax <>...</> can also be used.",
    ],
    example: ["<> ", "  <h1>Title</h1>", "  <p>Content</p>", "</>"],
    keyterms: [
      "Fragment = wrapper without extra DOM",
      "Clean DOM = no extra divs",
      "Short syntax = <>...</>",
    ],
  },
  {
    id: 64,
    question: "What is forwardRef() in React used for?",
    answer: [
      "forwardRef allows a parent to pass a ref to a child component.",
      "Useful when the parent needs direct access to a DOM element inside the child.",
      "Often used with input fields, custom UI components, or for focusing and animations.",
      "Works together with useRef to expose internal DOM nodes.",
    ],
    example: [
      "const Child = forwardRef((props, ref) => (",
      "  <input ref={ref} />",
      "));",
      " ",
      "const inputRef = useRef();",
      "<Child ref={inputRef} />",
    ],
    keyterms: [
      "forwardRef = pass ref to child",
      "DOM access = parent controls child's element",
      "useRef = holds the DOM reference",
    ],
  },
  {
    id: 65,
    question: "How do you reset a component's state in React?",
    answer: [
      "Resetting state means setting it back to its initial value.",
      "You can reset state by directly assigning the initial state.",
      "To fully reset a component (including its children), change its key prop to force React to remount it.",
      "For complex initial state, use a function form of useState to reuse the initialization logic.",
    ],
    example: [
      "// Child resets fully when key changes",
      "function Parent() {",
      "  const [key, setKey] = useState(0);",
      " ",
      "  return (",
      "    <>",
      "      <Child key={key} />",
      "      <button onClick={() => setKey(k => k + 1)}>Reset Child</button>",
      "    </>",
      "  );",
      "}",
    ],
    keyterms: [
      "State reset = restore initial value",
      "Key remount = force component to recreate",
      "Initial state function = re-run setup logic",
    ],
  },
  {
    id: 66,
    question:
      "Why does React recommend against mutating state or props directly?",
    answer: [
      "React relies on immutability to detect changes and trigger re-renders.",
      "Mutating state or props directly can cause React to skip updates or render stale UI.",
      "Direct mutation can lead to unexpected bugs and make debugging difficult.",
      "Always create a new object/array when updating state (e.g., using spread operator).",
    ],
    example: [
      "// Wrong: mutating state directly",
      "state.items.push('new');",
      " ",
      "// Correct: create new array",
      "setState(prev => [...prev, 'new']);",
    ],
    keyterms: [
      "Immutability = create new state instead of changing old",
      "Direct mutation = changes old object/array",
      "Shallow comparison = React uses for detecting changes",
      "Stale UI = UI may not update correctly",
    ],
  },
  {
    id: 67,
    question: "What are React performance optimization techniques?",
    answer: [
      "Use React.memo to prevent unnecessary re-renders of functional components.",
      "Use useCallback and useMemo to memoize functions and computed values.",
      "Avoid anonymous functions and objects in props to reduce re-renders.",
      "Use lazy loading and code-splitting to load components only when needed.",
      "Use virtualization (e.g., react-window) for long lists.",
      "Keep component state minimal and colocate state where needed.",
      "Avoid unnecessary state updates and re-renders.",
      "Use production build and enable React DevTools profiler to identify bottlenecks.",
    ],
    example: [
      "// Using React.memo",
      "const Child = React.memo(function ({ value }) {",
      "  return <p>{value}</p>;",
      "});",
      " ",
      "// Lazy loading",
      "const LazyComponent = React.lazy(() => import('./LazyComponent'));",
      " ",
      "// useMemo for expensive calculation",
      "const result = useMemo(() => heavyCalc(num), [num]);",
    ],
    keyterms: [
      "React.memo = memoizes functional components",
      "useCallback = memoizes functions",
      "useMemo = memoizes computed values",
      "Lazy loading = load component only when needed",
      "Virtualization = render only visible items in large lists",
      "Profiler = tool to measure performance",
    ],
  },
  {
    id: 68,
    question: "How do you implement an SVG image in a React project?",
    answer: [
      "SVG images can be used as React components or as <img> sources.",
      "Using them as components allows styling with props and CSS.",
      "Using <img> works like normal images but does not allow direct React styling or events.",
    ],
    example: [
      "// Import SVG as component",
      "import { ReactComponent as Logo } from './logo.svg';",
      " ",
      "function App() {",
      "  return <Logo width={100} height={100} />;",
      "}",
      " ",
      "// Using SVG as <img> source",
      "<img src='./logo.svg' alt='Logo' width={100} />",
    ],
    keyterms: [
      "SVG as component = allows props and styling",
      "<img> = static usage, cannot style via React",
      "ReactComponent import = special syntax from CRA",
      "Props = width, height, fill, className can be applied",
    ],
  },
  {
    id: 69,
    question:
      "Why do we need Redux Toolkit, and why not just use Redux or Context API for state management?",
    answer: [
      "Redux Toolkit (RTK) is the official, opinionated way to write Redux logic with less boilerplate.",
      "It simplifies store setup, reducers, and actions, reducing errors and repetitive code.",
      "It includes utilities like createSlice, createAsyncThunk, and configureStore for easier state management.",
      "Context API is good for simple or small apps, but Redux Toolkit scales better for large apps with complex state.",
      "RTK provides standardized patterns, better performance, and debugging tools like Redux DevTools.",
    ],
    example: [
      "// Using Redux Toolkit createSlice",
      "import { createSlice, configureStore } from '@reduxjs/toolkit';",
      " ",
      "const counterSlice = createSlice({",
      "  name: 'counter',",
      "  initialState: 0,",
      "  reducers: {",
      "    increment: state => state + 1,",
      "    decrement: state => state - 1",
      "  }",
      "});",
      " ",
      "const store = configureStore({ reducer: counterSlice.reducer });",
      "store.dispatch(counterSlice.actions.increment());",
      "console.log(store.getState()); // 1",
    ],
    keyterms: [
      "Redux Toolkit = simplifies Redux development",
      "createSlice = defines state + reducers + actions together",
      "configureStore = easier store setup",
      "Context API = simpler, good for small apps",
      "Scalability = RTK preferred for large, complex apps",
    ],
  },
  {
    id: 70,
    question:
      "What is the difference between Stateful and Stateless components in React?",
    answer: [
      "Stateful components (also called class components or functional components with state) manage and maintain their own state.",
      "They can have lifecycle methods (class components) or use hooks (functional components) to handle state and side effects.",
      "Stateless components (also called functional or presentational components) do not manage state; they receive data via props and render UI accordingly.",
      "Stateless components are simpler, easier to test, and reusable.",
    ],
    example: [
      "// Stateful component using useState",
      "function Counter() {",
      "  const [count, setCount] = useState(0);",
      "  return <button onClick={() => setCount(count + 1)}>{count}</button>;",
      "}",
      " ",
      "// Stateless component",
      "function Display({ message }) {",
      "  return <p>{message}</p>;",
      "}",
    ],
    keyterms: [
      "Stateful component = manages its own state",
      "Stateless component = no internal state, depends on props",
      "Reusability = stateless components are highly reusable",
      "Lifecycle/hooks = stateful components can handle side effects",
    ],
  },
  {
    id: 71,
    question:
      "Explain the browser storage system and how you implemented it in your React project.",
    answer: [
      "Browser gives LocalStorage, SessionStorage, and Cookies.",
      "LocalStorage → long-term storage (used to maintain theme, cart, auth token).",
      "SessionStorage → tab-scoped, so duplicating a tab only copies its data once, after which each tab has its own separate storage that doesn’t sync updates, and the data is removed when the tab is closed. (temp data)",
      "Cookies → small data sent automatically to server used for auth/session.",
      "In React, used for persisting login state, cart, theme, or recent actions.",
    ],
    example: [
      "// LocalStorage",
      "localStorage.setItem('theme', 'dark');",
      "const t = localStorage.getItem('theme');",
      " ",
      "// React usage example",
      "useEffect(() => {",
      "  localStorage.setItem('count', count);",
      "}, [count]);",
      " ",
      "// SessionStorage",
      "sessionStorage.setItem('tab', 'home');",
      " ",
      "// Cookie",
      "document.cookie = 'token=abc123';",
    ],
    keyterms: [
      "LocalStorage = persistent storage across reloads",
      "SessionStorage = per-tab temporary storage",
      "Cookies = small data sent with each request",
      "Persistence = keeping state after reload",
    ],
  },
  {
    id: 72,
    question: "Where should we store the JWT token in a React application?",
    answer: [
      "Best place is HTTP-only cookies because they are not accessible by JavaScript and safe from XSS.",
      "Tokens can also be kept in memory (React state/Redux) but get lost on page refresh.",
      "LocalStorage and SessionStorage are easy but unsafe because JS can access them, making tokens vulnerable to XSS.",
    ],
    example: [
      "// Storing JWT in HTTP-only cookie (backend sets it)",
      "res.cookie('token', jwt, { httpOnly: true, secure: true });",
      " ",
      "// Access token stored in memory",
      "const [token, setToken] = useState(null);",
    ],
    keyterms: [
      "HTTP-only Cookie = safest for JWT",
      "LocalStorage = persistent but XSS-prone",
      "SessionStorage = tab-only, still XSS-prone",
      "In-Memory Storage = safe from XSS, lost on refresh",
    ],
  },
  {
    id: 73,
    question:
      "How to handle query parameters and dynamic routing in React applications?",
    answer: [
      "Query parameters are accessed via URL search params (e.g., ?id=123).",
      "Dynamic routing allows paths to accept variables (e.g., /user/:id).",
      "In React Router, use `useParams()` for dynamic segments and `useSearchParams()` for query parameters.",
    ],
    example: [
      "// Define a dynamic route in React Router",
      "<Route path='/user/:id' element={<UserPage />} />",
      " ",
      "// Access dynamic segment inside component",
      "const params = useParams();",
      "const id = params.id; // gets the :id from URL",
      " ",
      "// Access query parameters",
      "const [searchParams] = useSearchParams();",
      "const page = searchParams.get('page'); // ?page=2",
      " ",
      "// You can now use 'id' and 'page' in your component logic",
    ],
    keyterms: [
      "Dynamic routing = URL paths with variables (e.g., /user/:id)",
      "Query params = ?key=value in URL",
      "useParams / useSearchParams = hooks to access route data in React Router",
    ],
  },
];

export default reactQuestions;
